#include <stdint.h>
#include <stdlib.h>
#include <stdio.h>
#include "vm.h"
/*
int main(){
	struct vm *vm;
	vm=malloc(sizeof(struct vm));
	resetvm(vm);

	//registar prog[NRAM]={
	//	INK1, 
	//	SAB1,
	//	P12,
	//	PA1,
	//	P2A,
	//	SRP,
	//	5,
	//	DBG,
	//	SKOR, 
	//	-7,
	//	HLT,
	//};

	registar prog[NRAM]={
		UCAO,
		361,
		DODA,
		FUN,
		8,
		VRA,
		SKOK,
		590,

		//f(n)

		DODBP,
		PSB,

		PSA,
		SABR,
		3,
		UCAA,
		UPOR,
		1,
		SRN,
		7,//kraj
		UC2O,
		1,
		PBS,
		VRBP,
		POV,

		DODA,
		ODUR,
		1,
		DODA,
		FUN,
		8,
		VRA,
		VRA,
		SAB2,


		PA2,

		PBS,
		VRBP,
		POV,


	};

	prog[599]=DBG;
	prog[600]=HLT;
	uram(vm, prog, NRAM);

	pokrenivm(vm, 0);

	free(vm);

	return 0;
}
*/
